// Local API key override — copy this file to "config.js" (same folder) and
// fill in your key below. "config.js" is listed in .gitignore, so it never
// gets committed or pushed; it stays on your machine only.
//
// This is entirely optional — the Settings page (gear icon) works too, and
// a key saved there always takes priority over this file. Use this file
// instead if you'd rather not type the key into a page, or if you want it
// to already be set the first time you open a video.
//
//   cp background/config.example.js background/config.js
//
// then edit config.js and reload the extension at chrome://extensions.

export const GEMINI_API_KEY = "";
